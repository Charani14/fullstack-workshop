function swap() {
let x= prompt("Enter first number:");
let y= prompt("Enter second number:");
x= x*y;
y=x/y;
x=x/y;
alert("After swapping: First number is "+x+" and Second number is "+y);
console.log("After swapping: First number is "+x+" and Second number is "+y);
}